name: Lint my app

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: golangci-lint
      uses: reviewdog/action-golangci-lint@v2
